/**
 * Common Auth Module
 * Provides authentication services for registration and token generation
 */

export { default } from './authService';
export type { RegisterPayload, RegisterResponse, AuthPayload, AuthResponse } from './authService';
